
/*
import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';
import { Ionicons } from '@expo/vector-icons';
import { useRouter } from 'expo-router';

export default function BottomTabs() {
  const router = useRouter(); // Navegação

  return (
    <View style={styles.container}>
      <TouchableOpacity style={styles.tab} onPress={() => router.push('/dashboard')}>
        <Ionicons name="home-outline" size={24} color="#FCD535" />
        <Text style={styles.label}>Início</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.tab} onPress={() => router.push('/operacoes')}>
        <Ionicons name="briefcase-outline" size={24} color="#FCD535" />
        <Text style={styles.label}>Operações</Text>
      </TouchableOpacity>

      <TouchableOpacity style={styles.tab} onPress={() => router.push('/atendimento')}>
        <Ionicons name="people-outline" size={24} color="#FCD535" />
        <Text style={styles.label}>Atendimento</Text>
      </TouchableOpacity>
    </View>
  );
}

// Estilos
const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    paddingVertical: 10,
    backgroundColor: '#121212', // Cor escura
    borderTopWidth: 1,
    borderTopColor: '#333',
  },
  tab: {
    alignItems: 'center',
  },
  label: {
    fontSize: 12,
    color: '#FCD535', // Amarelo Binance
    marginTop: 4,
  },
});
*/